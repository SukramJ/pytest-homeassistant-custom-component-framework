# setup.py reads these at build time; without them a wheel built from the
# sdist (as `python -m build` does) fails with FileNotFoundError.
include version
include ha_version
include requirements_test.txt
include README.md
include LICENSE
include LICENSE_HA_CORE.md
recursive-include src/pytest_homeassistant_custom_component/fixtures *.json
